# راهنمای PWA اختصاصی آرایشگران

این سند راهنمای کامل نصب و استفاده از اپلیکیشن جداگانه PWA برای آرایشگران است.

## 🎯 ویژگی‌های PWA آرایشگر

### ✨ امکانات اصلی
- **اپلیکیشن جداگانه**: PWA کاملاً مستقل از اپلیکیشن اصلی
- **اعلان‌های آنی**: دریافت اعلان فوری هنگام ثبت رزرو جدید
- **مدیریت رزروها**: مشاهده و مدیریت تمام رزروها
- **دسترسی آفلاین**: امکان مشاهده رزروها حتی بدون اینترنت
- **نصب آسان**: نصب روی صفحه اصلی موبایل با یک کلیک

### 📱 پشتیبانی از پلتفرم‌ها
- ✅ Android (Chrome, Firefox, Edge)
- ✅ iOS (Safari)
- ✅ Desktop (Chrome, Edge, Firefox)

## 🚀 نحوه نصب PWA آرایشگر

### برای اندروید (Android)

#### روش 1: از طریق داشبورد
1. وارد داشبورد خود شوید: `https://yourdomain.com/barber-dashboard/نام-آرایشگر`
2. روی دکمه "📱 نصب اپ" کلیک کنید
3. در پنجره باز شده، روی "نصب" یا "Install" بزنید
4. اپلیکیشن روی صفحه اصلی شما نصب می‌شود

#### روش 2: از طریق مرورگر
1. وارد صفحه ورود آرایشگر شوید: `https://yourdomain.com/barber-login`
2. روی منوی سه نقطه (⋮) در بالای مرورگر کلیک کنید
3. گزینه "افزودن به صفحه اصلی" یا "Install app" را انتخاب کنید
4. نام اپلیکیشن را تأیید کرده و روی "افزودن" بزنید

### برای iOS (آیفون/آیپد)

#### مراحل نصب
1. از **Safari** وارد صفحه ورود آرایشگر شوید
2. روی دکمه "📱 نصب اپ" کلیک کنید (راهنمای تصویری نمایش داده می‌شود)
3. روی دکمه **اشتراک‌گذاری** (⬆️) در **پایین صفحه** بزنید
4. از منوی باز شده، گزینه **"افزودن به صفحه اصلی"** را انتخاب کنید
5. نام "داشبورد [نام شما] - HRD" را تأیید کنید
6. روی **"افزودن"** بزنید

⚠️ **نکته مهم**: در iOS حتماً از Safari استفاده کنید (نه Chrome یا Firefox)

## 🔔 تنظیم اعلان‌های فوری

### فعال‌سازی اعلان‌ها

1. **اولین بار که وارد داشبورد می‌شوید**:
   - مرورگر از شما اجازه ارسال اعلان می‌خواهد
   - روی "اجازه" یا "Allow" بزنید

2. **اگر قبلاً رد کرده‌اید**:
   - در اندروید: Settings → Apps → Chrome → Notifications → مجوز بدهید
   - در iOS: Settings → Safari → روی اعلان‌ها مجوز بدهید

### انواع اعلان‌ها

#### 🎉 رزرو جدید
- **زمان**: فوراً بعد از ثبت رزرو توسط مشتری
- **محتوا**: نام مشتری، خدمات انتخابی، ساعت رزرو
- **صدا**: 3 بیپ متوالی برای جلب توجه
- **نمایش**: اعلان بر روی صفحه قفل و داخل اپلیکیشن

#### 📋 بروزرسانی رزرو
- به‌روزرسانی خودکار لیست رزروها هر 30 ثانیه
- نمایش بنر سبز هنگام دریافت رزرو جدید

## 📂 ساختار فایل‌های PWA

```
hrdbarber.shop/
├── public/
│   ├── barber-manifest.json      # مانیفست اختصاصی آرایشگر
│   ├── barber-sw.js              # Service Worker آرایشگر
│   └── icons/                    # آیکون‌های PWA
├── src/
│   ├── app/
│   │   ├── barber-app/           # صفحه ورودی PWA
│   │   │   └── page.tsx
│   │   ├── barber-login/         # صفحه ورود آرایشگر
│   │   │   └── page.tsx
│   │   ├── barber-dashboard/     # داشبورد آرایشگر
│   │   │   └── [barberId]/
│   │   │       └── page.tsx
│   │   └── api/
│   │       └── barber/
│   │           ├── subscribe/    # ثبت اشتراک اعلان
│   │           │   └── route.js
│   │           ├── notify/       # ارسال اعلان
│   │           │   └── route.js
│   │           └── [barberId]/   # API رزروهای آرایشگر
│   │               └── route.js
│   └── components/
│       └── BarberPWAInstall.tsx  # کامپوننت نصب PWA
└── data/
    └── barber-subscriptions.json # اشتراک‌های اعلان
```

## 🔧 API Endpoints

### 1. دریافت رزروهای آرایشگر
```http
GET /api/barber/[barberId]
```

**پارامترها**:
- `barberId`: نام آرایشگر (URL encoded)
- `date` (اختیاری): تاریخ خاص
- `status` (اختیاری): وضعیت رزرو

**پاسخ**:
```json
{
  "barber": "نام آرایشگر",
  "bookings": [...],
  "total_bookings": 10
}
```

### 2. ثبت اشتراک اعلان
```http
POST /api/barber/subscribe
```

**بدنه درخواست**:
```json
{
  "barberId": "نام آرایشگر",
  "subscription": {
    "endpoint": "...",
    "keys": {...}
  }
}
```

### 3. ارسال اعلان به آرایشگر
```http
POST /api/barber/notify
```

**بدنه درخواست**:
```json
{
  "barberId": "نام آرایشگر",
  "title": "عنوان اعلان",
  "body": "متن اعلان",
  "data": {
    "bookingId": "...",
    "date": "...",
    "time": "..."
  }
}
```

### 4. بروزرسانی وضعیت رزرو
```http
PUT /api/barber/[barberId]
```

**بدنه درخواست**:
```json
{
  "booking_id": "...",
  "status": "confirmed|cancelled|completed",
  "notes": "یادداشت اختیاری"
}
```

## ⚙️ پیکربندی Service Worker

### قابلیت‌های Service Worker آرایشگر

1. **کش کردن منابع**:
   - صفحات داشبورد
   - API responses
   - تصاویر و آیکون‌ها

2. **پشتیبانی آفلاین**:
   - نمایش آخرین رزروهای دریافت شده
   - امکان مشاهده اطلاعات بدون اینترنت

3. **همگام‌سازی پس‌زمینه**:
   - بروزرسانی خودکار رزروها وقتی اینترنت برمی‌گردد
   - ارسال تغییرات انجام شده در حالت آفلاین

4. **مدیریت اعلان‌ها**:
   - دریافت Push Notifications
   - نمایش اعلان‌های سیستمی
   - مدیریت کلیک روی اعلان‌ها

## 🎨 تم و رنگ‌بندی

PWA آرایشگر از رنگ‌بندی متفاوتی نسبت به اپلیکیشن اصلی استفاده می‌کند:

- **رنگ اصلی (Theme Color)**: `#f59e0b` (زرد/نارنجی)
- **رنگ پس‌زمینه**: `#1e293b` (خاکستری تیره)
- **طراحی**: گلس مورفیسم با افکت‌های بلور

## 🔐 امنیت و احراز هویت

### سیستم ورود
- احراز هویت با نام کاربری و رمز عبور
- ذخیره session در localStorage
- بررسی اعتبار session در هر درخواست

### حفاظت از داده‌ها
- دسترسی آرایشگر فقط به رزروهای خود
- اعتبارسنجی barberId در هر API call
- محدودیت دسترسی به اطلاعات مشتریان

## 🐛 عیب‌یابی

### اگر اعلان‌ها نمی‌آید:

1. **بررسی مجوز اعلان‌ها**:
   ```javascript
   console.log(Notification.permission);
   // باید 'granted' باشد
   ```

2. **بررسی ثبت Service Worker**:
   ```javascript
   navigator.serviceWorker.ready.then(registration => {
     console.log('SW registered:', registration);
   });
   ```

3. **بررسی اشتراک Push**:
   - به `/api/barber/notify` مراجعه کنید
   - لیست آرایشگران ثبت‌شده را بررسی کنید

### اگر PWA نصب نمی‌شود:

**اندروید**:
- مطمئن شوید از HTTPS استفاده می‌کنید
- کش مرورگر را پاک کنید
- Chrome را بروزرسانی کنید

**iOS**:
- حتماً از Safari استفاده کنید
- iOS 11.3 یا بالاتر داشته باشید
- دکمه Share را در پایین صفحه پیدا کنید

### اگر رزروها بروز نمی‌شوند:

1. دکمه Refresh را در داشبورد فشار دهید
2. اتصال اینترنت خود را بررسی کنید
3. از اپلیکیشن خارج شوید و دوباره وارد شوید
4. کش مرورگر را پاک کنید

## 📊 نظارت و آمار

### اطلاعات قابل مشاهده در داشبورد:

- **کل رزروها**: تعداد کل رزروهای دریافتی
- **رزروهای امروز**: رزروهای امروز که لغو نشده‌اند
- **فیلترها**: بر اساس تاریخ و وضعیت
- **وضعیت رزروها**: در انتظار، تأیید شده، لغو شده، تکمیل شده

### اقدامات قابل انجام:

- ✅ تأیید رزرو
- ❌ لغو رزرو
- 🎉 علامت‌گذاری به عنوان تکمیل شده
- 📝 افزودن یادداشت

## 🔄 بروزرسانی PWA

PWA به صورت خودکار بروزرسانی می‌شود:

1. هنگام بازکردن اپلیکیشن، آخرین نسخه دانلود می‌شود
2. تغییرات در دفعه بعدی اجرا اعمال می‌شوند
3. پیامی برای بروزرسانی نمایش داده نمی‌شود (بدون وقفه)

### بروزرسانی دستی:
- از اپلیکیشن خارج شوید
- اپلیکیشن را دوباره باز کنید
- یا از داشبورد، دکمه "بارگذاری مجدد" را بزنید

## 🌐 پیکربندی برای Production

### متغیرهای محیطی مورد نیاز:

```env
NEXT_PUBLIC_BASE_URL=https://yourdomain.com
MONGODB_URI=mongodb://...
```

### نکات مهم:

1. **HTTPS الزامی است** برای PWA و Push Notifications
2. **VAPID Keys**: برای Push Notification واقعی باید کلیدهای VAPID تولید کنید:
   ```bash
   npx web-push generate-vapid-keys
   ```
3. **Domain Verification**: دامنه را در Google Play Console تأیید کنید

## 📱 تست PWA

### چک‌لیست تست:

- [ ] نصب PWA روی اندروید موفق است
- [ ] نصب PWA روی iOS موفق است  
- [ ] اعلان‌ها در اندروید کار می‌کند
- [ ] اعلان‌ها در iOS نمایش داده می‌شود
- [ ] رزروها به درستی نمایش داده می‌شوند
- [ ] فیلترها صحیح عمل می‌کنند
- [ ] بروزرسانی خودکار رزروها کار می‌کند
- [ ] تغییر وضعیت رزرو امکان‌پذیر است
- [ ] حالت آفلاین به درستی کار می‌کند
- [ ] صدای اعلان پخش می‌شود

### ابزارهای تست:

1. **Chrome DevTools**:
   - Application → Manifest
   - Application → Service Workers
   - Application → Notifications

2. **Lighthouse**:
   - بررسی PWA Score
   - بررسی Performance
   - بررسی Best Practices

## 💡 نکات مفید

### بهینه‌سازی تجربه کاربری:

1. **صدای اعلان**: سه بیپ برای جلب بهتر توجه
2. **بنر بصری**: نمایش بنر سبز در صفحه برای رزروهای جدید
3. **بروزرسانی خودکار**: هر 30 ثانیه بررسی رزروهای جدید
4. **نمایش تعداد**: تعداد رزروهای جدید نمایش داده می‌شود

### پیشنهادها:

- اپلیکیشن را همیشه باز نگه دارید تا اعلان‌ها را از دست ندهید
- صدای گوشی را روشن نگه دارید
- مجوز اعلان‌ها را حتماً بدهید
- در صورت مشکل، لاگ‌های console را بررسی کنید

## 🆘 پشتیبانی

در صورت بروز مشکل:

1. **بررسی Console Logs**: 
   - F12 → Console
   - پیام‌های خطا را بررسی کنید

2. **تماس با پشتیبانی**:
   - ایمیل: support@hrdbarber.shop
   - تلگرام: @hrdbarber_support

3. **گزارش مشکل**:
   - اسکرین‌شات از خطا
   - مدل گوشی و نسخه مرورگر
   - توضیح کامل مشکل

---

**نسخه**: 1.0.0  
**تاریخ بروزرسانی**: اکتبر 2025  
**سازنده**: HRD Barber Shop Team

